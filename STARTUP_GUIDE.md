# Personal Health 完整啟動與測試指南

## 🚀 啟動步驟

### 1️⃣ 啟動後端 API（終端 1）
```powershell
cd C:\Users\User\Desktop\personalhealth
.venv\Scripts\activate
python start_backend.py
```

**預期輸出：**
```
🚀 Personal Health Backend API
📍 URL: http://localhost:8000
INFO: Uvicorn running on http://0.0.0.0:8000
INFO: Application startup complete.
```

---

### 2️⃣ 啟動 Streamlit Web UI（終端 2）
```powershell
cd C:\Users\User\Desktop\personalhealth
.venv\Scripts\activate
streamlit run frontend/main.py
```

**預期輸出：**
```
You can now view your Streamlit app in your browser.
Local URL: http://localhost:8501
```

---

### 3️⃣ 測試完整流程（終端 3，可選）
```powershell
cd C:\Users\User\Desktop\personalhealth
.venv\Scripts\activate
python test_complete_flow.py
```

---

## ✅ 驗證服務狀態

### 方法 1：瀏覽器訪問
- **Streamlit Web UI**: http://localhost:8501
- **API 文檔**: http://localhost:8000/docs
- **健康檢查**: http://localhost:8000/health

### 方法 2：PowerShell 命令
```powershell
# 測試後端
curl http://localhost:8000/health

# 測試 Streamlit
curl http://localhost:8501
```

---

## 📱 使用 Streamlit Web UI

1. 打開瀏覽器：http://localhost:8501

2. **步驟 1：個人資料**
   - 填寫姓名、年齡、身高、體重
   - 選擇活動量、飲食偏好
   - 點擊「下一步：上傳報告」

3. **步驟 2：上傳報告**
   - 上傳健檢報告圖片（JPG/PNG）
   - AI 會自動辨識數據
   - 等待分析完成（約 30 秒）

4. **步驟 3：健康儀表板**
   - 查看健康評分
   - 瀏覽異常指標
   - 閱讀營養建議
   - 與 AI 營養師對話

5. **步驟 4：營養查詢**（新增！）
   - 搜尋食物名稱（例如：雞胸肉）
   - 查看完整營養成分
   - 2,180 種台灣食物資料庫

---

## 🛠️ 故障排除

### 後端無法啟動
```powershell
# 檢查 port 8000 是否被占用
netstat -ano | findstr :8000

# 如果被占用，終止進程
taskkill /PID <PID> /F
```

### Streamlit 無法啟動
```powershell
# 檢查 port 8501 是否被占用
netstat -ano | findstr :8501

# 清理所有 streamlit 進程
Get-Process | Where-Object {$_.ProcessName -like "*streamlit*"} | Stop-Process -Force
```

### 連線錯誤：Server disconnected
**原因**：後端 API 沒有運行或被關閉

**解決方法**：
1. 確認終端 1 的後端仍在運行
2. 看到 `INFO: Application startup complete.` 表示正常
3. 如果關閉了，重新執行步驟 1️⃣

---

## 📊 系統架構

```
┌─────────────────┐
│   瀏覽器         │
│  localhost:8501 │
└────────┬────────┘
         │ HTTP
         ▼
┌─────────────────┐
│  Streamlit UI   │
│   (frontend)    │
└────────┬────────┘
         │ REST API
         ▼
┌─────────────────┐      ┌──────────────┐
│  FastAPI         │─────▶│  Gemini AI   │
│  Backend API     │      │  (OCR+Chat)  │
│  localhost:8000  │      └──────────────┘
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  SQLite DB      │
│  sql_app.db     │
└─────────────────┘
```

---

## 🎯 重要提示

1. **先啟動後端，再啟動 Streamlit**
2. **保持兩個終端視窗開啟**
3. **使用 Ctrl+C 停止服務**
4. **測試腳本不會影響正在運行的服務**

---

**建立日期**: 2026-01-17  
**維護者**: Personal Health Team
