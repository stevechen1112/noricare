import 'package:flutter/material.dart';
import '../models/meal.dart';

/// Stub for MealPhotoSuggestPage on Web
class MealPhotoSuggestPage extends StatelessWidget {
  const MealPhotoSuggestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('拍照辨識')),
      body: const Center(
        child: Text('Web 版本不支援拍照功能'),
      ),
    );
  }
}
